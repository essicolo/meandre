# --- RÈGLES CRITIQUES (Tes dossiers gpkg) ---
.gpkg/
*.qgz
*.gpkg

# --- PYTHON ---
__pycache__/
*.py[cod]
*$py.class
.venv/
.pytest_cache/
fdc_training.log

# --- DONNÉES ET RÉSULTATS (Pour éviter l'erreur Permission Denied) ---
*.duckdb
*.duckdb.wal
*.nc
*.nc.bak*
*.tif
*.sqlite3
Hydat_sqlite3_*.zip
*.npy
notebooks/slso/data/geo_cache/
notebooks/slso/results/

# --- ALL EXPERIMENTS / CASES ---
# Each case has its own folder under .models/<case-name>/ with config,
# data (basin.duckdb, forcing.nc, geo_cache/), checkpoints, results.
# .models/_shared/ holds caches reusable across cases (HYDAT SQLite, etc.)
.models/

# --- CONFIG ET SAUVEGARDES ---
gitignore_bak
.vscode/
.claude/

*.pt
*.pth
*.duckdb